plugins {
    id 'application'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(16)
    }
}

application {
    mainClass.set('net.jbock.convert.GenAutoTypes')
}

repositories {
    mavenCentral()
    maven { url 'https://s01.oss.sonatype.org/content/repositories/iogithubjbock-java-1030' }
}

dependencies {
    implementation 'io.github.jbock-java:jbock:5.4'
    implementation 'io.github.jbock-java:jbock-compiler:5.4'
    implementation 'com.google.testing.compile:compile-testing:0.19'
    implementation 'org.mockito:mockito-core:3.9.0'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.7.1'
}

test {
    useJUnitPlatform()
    testLogging {
        events 'failed'
    }
}

